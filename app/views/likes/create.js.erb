$('.likes-count-<%=@post.id%>').text(<%=@post.likes.count%> + " Likes")
$('#likes-<%=@post.id%>').html('<%= j link_to ('<i class="fa fa-thumbs-up text-capitalize"> Like</i>').html_safe, like_path(@post), method: :delete, remote: true, class:"card-link" %>')